#include <iostream>
#include <array>

int main(int argc, char const *argv[]){
  std::vector<double> scores {};
  int add_score;
  std::cout << "Enter a test score: ";
  if(add_score >= 0){
    double entered_score = 0;
    cin << entered_score;
    std::scores.push_back(entered_score);
  }
  for(int i=0; i < scores.size(); ++i){
    std::cout << scores.at(i) << "";
  }
  return = 0;
}